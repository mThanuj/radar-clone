{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "crons": [
    { "path": "/api/cron/email", "schedule": "*/5 * * * *" },
    { "path": "/api/cron/due-soon", "schedule": "0 9 * * *" }
  ]
}
